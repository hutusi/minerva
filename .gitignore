node_modules/
dist/
coverage/
*.log
.env
.env.*
.DS_Store
# Project-layer settings written by running the minerva TUI inside this repo
# (persisted "always allow" rules) — local dev state, not shared config.
.minerva/
# Tauri GUI build state; binaries/ holds the prepared sidecar pair (host-built).
apps/gui/src-tauri/target/
apps/gui/src-tauri/binaries/
apps/gui/src-tauri/gen/schemas/
# Harbor eval adapter (Python island): venv, build, and local run artifacts.
evals/harbor/.venv/
evals/harbor/**/__pycache__/
evals/harbor/*.egg-info/
evals/harbor/results/
